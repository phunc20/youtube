# Why Immutable Data Structure?
Functional programmming prefers immutable ds because it thinks that in that way
when multiple threads access to the same data, they won't need to worry if the
data is being modified by any other threads any more.


## Q1
namedtuple's element if mutable are mutabel?
